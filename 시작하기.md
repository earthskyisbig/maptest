# 서울 아파트 경매 지도 — 수강생 시작 키트

이 압축파일 하나로 강의에서 본 지도(`seoul_auction_map.html`)를 **바로 열어 보고**, 원하면 **데이터를 새로 받아 다시 만들** 수 있습니다.

## 1. 바로 열어 보기 (5분)

1. 압축을 풀고 폴더 안에서 터미널(PowerShell)을 엽니다.
2. 아래 한 줄을 실행합니다. (지도 파일은 인터넷 주소 형태로 열어야 카카오 키 등록과 맞습니다.)
   ```
   python -m http.server 8000
   ```
3. 브라우저에서 `http://localhost:8000/seoul_auction_map.html` 을 엽니다.
4. 우측 상단 🔑 버튼 → **카카오 JavaScript 키** 입력 → 저장. 지도가 나타납니다.
   - 키 발급: developers.kakao.com → 내 애플리케이션 → 앱 키 → **JavaScript 키**
   - 같은 화면의 플랫폼 > Web > 사이트 도메인에 **`http://localhost:8000`** 을 등록해야 합니다. 이걸 빠뜨리면 회색 화면만 나옵니다.
   - 브이월드맵 배경까지 쓰려면 vworld.kr 오픈API **인증키**도 같은 곳에 입력 (선택)

| 파일 | 내용 |
|---|---|
| `seoul_auction_map.html` | 최종 지도: 서울 아파트 경매물건 + 서울플랜+ 정비구역 + 정비예정구역 + 공시가격·건축물 정보 |
| `vworld_four_map.html` | VWorld 주제도 4종(수도권정비권역·정비구역·재정비촉진지구·정비예정구역) 통합 지도 |

## 2. Claude Code로 따라 만들기

1. Claude Code를 설치·로그인하고 이 폴더에서 실행합니다. `.claude/` 폴더(에이전트·스킬)와 `CLAUDE.md`가 함께 들어 있어 **하네스는 이미 구성된 상태**입니다.
2. 강의에서 본 프롬프트를 순서대로 입력하세요. 파일 경로(`C:\Users\<me>\Downloads\...`)만 본인 것으로 바꿉니다.
3. 재료(공공 데이터)는 아래에서 받습니다.

| 데이터 | 어디서 | 파일명 예 |
|---|---|---|
| 정비구역·재정비촉진지구·정비예정구역·수도권정비권역 | vworld.kr → 공간정보 다운로드 → 주제도 검색 "정비" | `AL_D029_00_YYYYMMDD.zip`, `AL_D314…`, `AL_D316…`, `AL_D014…` |
| 서울플랜+ 도시계획사업 | 서울열린데이터광장 / 서울 도시계획포털 | `UQ120_도시계획사업(서울플랜+)_YYYYMM.zip` |
| 공동주택 공시가격 | vworld.kr → 공동주택가격정보 (D167, 서울=11) | `AL_D167_11_YYYYMMDD.zip` (CSV) |
| GIS건물통합정보 | vworld.kr → GIS건물통합정보 (D010, 서울=11) | `AL_D010_11_YYYYMMDD.zip` |
| 경매물건 | 스크립트가 법원경매정보 사이트에서 직접 수집 | 다운로드 불필요 |

## 3. 스크립트로 직접 갱신하기 (Claude 없이)

```
pip install -r requirements.txt

# 구역 (VWorld SHP zip → GeoJSON)
python scripts/shp_to_geojson.py "다운로드/AL_D029_00_YYYYMMDD.zip" _workspace/layer_zone_redev_vworld.geojson
python scripts/seoulplan_to_geojson.py "다운로드/UQ120_도시계획사업(서울플랜+)_YYYYMM.zip"

# 경매물건 수집 → 정규화·DuckDB → 지도
python scripts/collect_auction_seoul.py --days 60
python scripts/load_auction_duckdb.py
python scripts/build_auction_map.py

# (선택) 공시가격·건물정보를 팝업에 붙이기 — 먼저 적재한 뒤 load_auction_duckdb.py 를 다시 실행
python scripts/load_apt_price.py "다운로드/AL_D167_11_YYYYMMDD.zip"
python scripts/load_buildings.py "다운로드/AL_D010_11_YYYYMMDD.zip"
python scripts/load_auction_duckdb.py && python scripts/build_auction_map.py
```

- 좌표가 없는 경매물건을 보완할 때만 VWorld 지오코딩 키가 필요합니다. `.env.example`을 `.env`로 복사해 `VWORLD_API_KEY=` 에 넣으세요. (이번 데이터는 법원 좌표가 있어 0건이었습니다.)
- `data/estate.duckdb`는 스크립트가 만듭니다. 이 키트에는 용량(150MB) 때문에 들어 있지 않습니다.

## 4. 폴더 구조

```
.claude/agents, .claude/skills   에이전트 5 · 스킬 6 (하네스)
scripts/                          변환·수집·적재·지도 빌드 스크립트 8개
_workspace/                       레이어 GeoJSON (강의 당시 데이터, 지도 재빌드용)
seoul_auction_map.html            최종 지도
vworld_four_map.html              주제도 통합 지도
CLAUDE.md                         하네스 규칙과 변경 이력
```

## 5. 막히면

| 증상 | 해결 |
|---|---|
| 지도가 회색, "SDK 로드 실패" | 카카오 키 오타 또는 Web 도메인에 `http://localhost:8000` 미등록 |
| `file://`로 열었더니 안 됨 | 반드시 `python -m http.server 8000` 으로 열기 |
| 한글이 깨짐 | SHP는 `encoding="cp949"`로 읽어야 함 (스크립트에 이미 반영) |
| 경매 수집이 0건 | 매각기일 범위(`--days`)를 늘리거나, 사이트 접속 차단 시 잠시 후 재시도 |
